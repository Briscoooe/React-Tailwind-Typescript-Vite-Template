const config = {
  darkMode: "class",
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
      },
    },
  },
  variants: {
    extend: {
      display: ["group-hover"],
    },
  },
};

export default config;
